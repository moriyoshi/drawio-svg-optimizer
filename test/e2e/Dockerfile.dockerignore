# The build context is the repo root so `dist/` can be copied in, but almost
# nothing else there is wanted — and `node_modules` alone would add hundreds of
# megabytes to every context upload.
*
!dist
!test/e2e/probe.mjs
!test/e2e/entrypoint.sh
